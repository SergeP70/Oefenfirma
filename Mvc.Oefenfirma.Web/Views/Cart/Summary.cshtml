@model Mvc.Oefenfirma.Web.Models.ShoppingCart


    @Html.ActionLink("Your cart: " + @Model.Lines.Sum(x => x.Quantity) + " item(s) " + @Model.ComputeTotalValue().ToString("c"), "Index", "Cart",
    new { returnUrl = Request.Url.PathAndQuery },
    new { @class="btn btn-info navbar-btn"})

